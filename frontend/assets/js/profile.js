import {apiGet,apiPut,apiPost,escapeHTML,icons,toast} from './api.js';
const me=await apiGet('/auth/me').catch(()=>null);if(!me)location.assign('/admin/login.html');
const setAvatar=avatar=>{if(!avatar)return;const image=document.querySelector('.profile-photo');if(image)image.src=avatar;document.querySelectorAll('.admin-user .avatar').forEach(node=>{node.classList.add('avatar-image');node.innerHTML=`<img src="${escapeHTML(avatar)}" alt="Photo de profil">`;});};
const photo=document.querySelector('.profile-photo');if(photo){const wrap=document.createElement('div');wrap.className='profile-photo-wrap';photo.replaceWith(wrap);wrap.append(photo);const label=document.createElement('label');label.className='avatar-edit';label.innerHTML='<i data-lucide="camera" aria-hidden="true"></i><span>Changer la photo</span><input type="file" accept="image/jpeg,image/png,image/webp" hidden>';wrap.append(label);label.querySelector('input').addEventListener('change',async event=>{const file=event.currentTarget.files?.[0];if(!file)return;if(!['image/jpeg','image/png','image/webp'].includes(file.type)||file.size>4.5*1024*1024){toast('Choisissez une image JPG, PNG ou WebP de 4,5 Mo maximum.',true);event.currentTarget.value='';return;}const form=new FormData();form.set('avatar',file);label.classList.add('is-uploading');try{const data=await apiPost('/settings/avatar',form);setAvatar(data.avatar);toast('Photo de profil mise à jour.');}catch(error){toast(error.message,true);}finally{label.classList.remove('is-uploading');event.currentTarget.value='';}});}
if(me){document.querySelector('#profile-name').textContent=me.name;document.querySelector('#profile-email').textContent=me.email;setAvatar(me.avatar);}
document.querySelectorAll('[data-password]').forEach(button=>button.addEventListener('click',async()=>{const current_password=prompt('Mot de passe actuel');const new_password=prompt('Nouveau mot de passe (12 caractères minimum)');if(!current_password||!new_password)return;try{await apiPut('/settings/password',{current_password,new_password});location.assign('/admin/login.html');}catch(error){alert(error.message);}}));icons();
